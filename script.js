// make a line
// make a square
// make a pyramid


let lineMaker = function(num) {
  let line = ''
  for (let i = 0; i < num; i += 1) {
    line += 'x'
  }
  return line;
}

console.log(lineMaker(3))

let makeSquare = function(num) {
 
}

